# 🚀 Инструкция по запуску BASE Dash

## ✅ Сборка успешна!

Проект готов к запуску. Следуйте инструкциям ниже.

---

## 1️⃣ Локальный запуск (разработка)

### Шаг 1: Настройка окружения

```bash
# Скопируйте файл окружения
cp .env.example .env.local
```

### Шаг 2: Деплой смарт-контракта (testnet)

Для работы игры нужно развернуть смарт-контракт в сети Base Sepolia:

```bash
# 1. Добавьте приватный ключ в .env.local
# PRIVATE_KEY=your_private_key_here

# 2. Задеплойте контракт
npm run deploy:base-sepolia

# 3. Скопируйте адрес контракта из вывода
# 4. Вставьте в .env.local:
# NEXT_PUBLIC_CONTRACT_ADDRESS=0x...
```

### Шаг 3: Запуск сервера разработки

```bash
npm run dev
```

Откройте **http://localhost:3000** в браузере.

---

## 2️⃣ Публикация в production

### Шаг 1: Деплой на Vercel

```bash
# Установите Vercel CLI
npm install -g vercel

# Деплой
vercel

# Следуйте инструкциям в терминале
```

### Шаг 2: Настройка переменных окружения в Vercel

В панели управления Vercel добавьте:
- `NEXT_PUBLIC_APP_URL` - URL вашего приложения (например, `https://base-dash.vercel.app`)
- `NEXT_PUBLIC_CONTRACT_ADDRESS` - адрес развёрнутого контракта
- `NEXT_PUBLIC_USE_TESTNET` - `true` для testnet, `false` для mainnet

### Шаг 3: Настройка Base Mini App

1. Перейдите на [base.dev/account-association](https://base.dev/account-association)
2. Введите URL вашего приложения
3. Сгенерируйте учётные данные `accountAssociation`
4. Обновите `minikit.config.ts`:

```typescript
export const minikitConfig = {
  accountAssociation: {
    header: "your-header",
    payload: "your-payload",
    signature: "your-signature"
  },
  // ... остальные настройки
}
```

5. Обновите все URL в `app/.well-known/farcaster.json` на ваш production домен

### Шаг 4: Публикация

Создайте пост в Base app с URL вашего приложения для публикации.

---

## 3️⃣ Тестирование игры

### Подключение кошелька

1. Установите расширение Coinbase Wallet
2. Создайте кошелёк или импортируйте существующий
3. Переключитесь на сеть **Base Sepolia** (для тестов)
4. Получите тестовые ETH из крана: [Base Sepolia Faucet](https://www.coinbase.com/faucets/base-ethereum-faucet)

### Daily Check-in

1. Нажмите "Connect Wallet"
2. Нажмите "Check In Now" в разделе Daily Check-In
3. Подтвердите транзакцию в кошельке
4. После подтверждения ваш статус станет "Active"

### Игра

1. Перейдите на вкладку "Game"
2. Нажмите "Play" или Space для старта
3. Нажимайте Space/клик для прыжков
4. Избегайте красных шипов и белых блоков
5. После game over счёт будет отправлен в лидерборд (если активен check-in)

### Лидерборд

1. Перейдите на вкладку "Leaderboard"
2. Нажмите "Refresh" для обновления данных
3. Ваш счёт появится в таблице если он достаточно высок

---

## 4️⃣ Основные команды

```bash
# Разработка
npm run dev              # Запуск сервера разработки

# Сборка
npm run build            # Production сборка
npm run start            # Запуск production сервера

# Смарт-контракты
npm run compile          # Компиляция контрактов
npm run deploy:base-sepolia  # Деплой на Base Sepolia
npm run deploy:base          # Деплой на Base mainnet
npm run verify             # Верификация контракта

# Линтинг
npm run lint             # Проверка кода
```

---

## 5️⃣ Структура файлов

```
base-dash/
├── app/
│   ├── api/              # API endpoints
│   ├── components/       # React компоненты
│   ├── contracts/        # ABI и типы контрактов
│   ├── hooks/            # Custom React хуки
│   ├── lib/              # Утилиты и конфигурации
│   ├── styles/           # Глобальные стили
│   └── page.tsx          # Главная страница
├── contracts/
│   └── GameLeaderboard.sol  # Смарт-контракт
├── scripts/
│   └── deploy.ts         # Скрипт деплоя
├── public/               # Статические файлы
├── .env.example          # Шаблон переменных окружения
├── README.md             # Документация
└── package.json          # Зависимости
```

---

## 6️⃣ Решение проблем

### Ошибка "Contract not deployed"

- Убедитесь что контракт развёрнут
- Проверьте `NEXT_PUBLIC_CONTRACT_ADDRESS` в `.env.local`

### Ошибка "Wallet not connected"

- Установите Coinbase Wallet
- Подключитесь к сети Base Sepolia
- Получите тестовые ETH

### Ошибка при сборке

```bash
# Очистите кэш
rm -rf .next node_modules
npm install
npm run build
```

### Транзакция не подтверждается

- Проверьте баланс ETH
- Увеличьте gas price в кошельке
- Убедитесь что подключены к правильной сети

---

## 7️⃣ Полезные ссылки

- [Base Documentation](https://docs.base.org)
- [Base Sepolia Faucet](https://www.coinbase.com/faucets/base-ethereum-faucet)
- [Basescan Sepolia](https://sepolia.basescan.org)
- [Wagmi Docs](https://wagmi.sh)
- [Next.js Docs](https://nextjs.org)

---

## 🎮 Готово!

Теперь у вас есть рабочая игра BASE Dash с on-chain лидербордом!

**Удачи в разработке! 🚀**
